{
  "name": "misa-web",
  "version": "1.0.0",
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "deploy": "npm run build && wrangler deploy",
    "preview": "npm run build && wrangler dev"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.5",
    "wrangler": "^4.77.0"
  },
  "dependencies": {
    "@directus/sdk": "^21.2.0",
    "dotenv": "^17.3.1"
  }
}
